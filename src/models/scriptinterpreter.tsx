
// parses the script into a list of commands
// sends them all to the command interpreter


const ScriptInterpreter = (editorEnabled: boolean) => {
    return '';
}

export default ScriptInterpreter;