[

{
    "id": 0,
    "section": "Selection",
    "methods": [
        {"mid":0, "method":"select(square)", "params":"square - where the piece you want to select is", "desc":"selects a piece at the given position if one exists and returns or fale otherwise.", "example":"select(G5)"},
        {"mid":1, "method":"selected()", "params":"none", "desc":"returns the currently selected piece or false if none exists.", "example":"select()"},
        {"mid":2, "method":"unselect()", "params":"none", "desc":"unselects the currently selected piece if one exists and returns false otherwise.", "example":"unselect()"}
    ]
},
{
    "id": 1,
    "section": "Movement",
    "methods": [
        {"mid":0, "method":"move(from, to)", "params":"from - square piece to move is on, to - destination square", "desc":"moves the currently selected piece to the given position iff it is a valid move and false otherwise.", "example":"move(C3,F6)"},
        {"mid":0, "method":"move(to)", "params":"to - destination square", "desc":"moves the currently selected piece to the given position iff it is a valid move and false otherwise. *only works when a piece is selected", "example":"move(E4)"},
        {"mid":1, "method":"take(from, to)", "params":"from - square piece to move is on, to - destination square", "desc":"like move but only works when said move captures another piece.", "example":"take(G1,C5)"},
        {"mid":1, "method":"take(to)", "params":"to - destination square", "desc":"like move but only works when said move captures another piece. *only works when a piece is selected", "example":"take(D3)"}
    ]
},
{
    "id": 2,
    "section": "Checking",
    "methods": [
        {"mid":0, "method":"isValidMove(from, to)", "params":"from - square piece to move is on, to - destination square", "desc":"returns true if the given position is a vlid move for the selected piece if one exists and false otherwise.", "example":"isValidMove(B2,H3)"},
        {"mid":0, "method":"isValidMove(to)", "params":"to - destination square", "desc":"returns true if the given position is a vlid move for the selected piece if one exists and false otherwise. *only works when a piece is selected", "example":"isValidMove(D7)"},
        {"mid":1, "method":"showValidMoves()", "params":"none", "desc":"shows all the valid moves of the selected piece if one exists and false otherwise.", "example":"showValidMoves()"},
        {"mid":2, "method":"hideValidMoves()", "params":"none", "desc":"hides all the valid moves of the selected piece if one exists and false otherwise.", "example":"hideValidMoves()"},
        {"mid":3, "method":"showMoveHistory()", "params":"none", "desc":"prints the move history for both players in the terminal or returns false if no moves have been played.", "example":"showMoveHistory()"},
        {"mid":4, "method":"showWhiteHistory()", "params":"none", "desc":"prints the move history for white in the terminal or returns false if no moves have been played.", "example":"showWhiteHistory()"},
        {"mid":5, "method":"showBlackHistory()", "params":"none", "desc":"prints the move history for black in the terminal or returns false if no moves have been played.", "example":"showBlackHistory()"}
    ]
},
{
    "id": 3,
    "section": "Gameplay",
    "methods": [
        {"mid":0, "method":"offerDraw()", "params":"none", "desc":"offers a draw to the opponent.", "example":"offerDraw()"},
        {"mid":1, "method":"resign()", "params":"none", "desc":"resigns the game.", "example":"resign()"},
        {"mid":2, "method":"getEvaluation()", "params":"none", "desc":"returns a number between x and y. more positive is in favour of white, more negative is in favour of black.", "example":"getEvaluation()"}
    ]
},
{
    "id": 4,
    "section": "Other",
    "methods": [
        {"mid":0, "method":"help(method)", "params":"method signature without parameters", "desc":"displays use case information for the given method so you don't have to come to the docs.", "example":"help(take)"},
        {"mid":1, "method":"clear()", "params":"none", "desc":"clears the terminal.", "example":"clear()"}
    ]
},
{
    "id": 4,
    "section": "Secret",
    "methods": [
        {"mid":0, "method":"hint()", "params":"none", "desc":"tells you the square of the piece the current bot would chose if they were you.", "example":"hint()"},
        {"mid":1, "method":"hint(square)", "params":"square - recommend square to move the piece", "desc":"tells you the square the current bot would move the given piece if they were you.", "example":"hint(G2)"}
    ]
}

]